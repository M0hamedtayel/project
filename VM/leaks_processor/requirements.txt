# Breach Processing Engine — Python Dependencies
# Install: pip install -r requirements.txt

# Core
requests>=2.31.0

# RabbitMQ
pika>=1.3.2

# Elasticsearch (direct HTTP — no official client needed)
# requests is used for all ES interactions

# Archive extraction
rarfile>=4.1       # RAR support (requires unrar binary on system)
py7zr>=0.20.0     # 7z support

# Linux file watching (for live stream mode)
inotify-simple>=1.3.0

# AI column detection (optional — requires Ollama running locally)
# No pip package needed — uses Ollama REST API via requests
